@model ProjetFinal_2147037.Models.ImageUploadVM

<a asp-action="Index">Retour à l'index</a>
<h2>Ajouter une image à un fruit</h2>

<form asp-action="AjouterImage" enctype="multipart/form-data">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <div class="form-group">
        <label asp-for="NomUtil" class="control-label"></label>
        <input asp-for="NomUtil" class="form-control" />
        <span asp-validation-for="NomUtil" class="text-danger"></span>
    </div>
    <div class="form-group">
        <!-- Ajouter un input pour le fichier image ici -->
        <label for="FormFile">Fichier image: </label>
        <input id="FormFile" name="FormFile" type="file" accept="image/*" class="form-control-file" />
        <span asp-validation-for="FormFile" class="text-danger"></span>
    </div>
    <div class="form-group">
        <input type="submit" value="Téléverser" class="btn btn-primary" />
    </div>
</form>